#!/usr/bin/env perl

my $brightness = `cat /sys/class/backlight/amdgpu_bl0/actual_brightness`;

$brightness -= 5;
$brightness = 1 if $brightness < 1;

my $junk = `echo $brightness > /sys/class/backlight/amdgpu_bl0/brightness`;

